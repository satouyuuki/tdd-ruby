独立したテストケースを描こう
ダメな例
def test_数字を渡したら文字列を返す
  assert_equal '1', FizzBuzz.generate(1)
  assert_equal '2', FizzBuzz.generate(2)
  assert_equal '3', FizzBuzz.generate(3)
  assert_equal '4', FizzBuzz.generate(4)
  assert_equal '5', FizzBuzz.generate(5)
end


# リファクタフェイズ
メソッドの抽出 を適用して重複を除去しましょう。
インスタンス変数 に FizzBuzz クラス の参照を 代入

変数名を変える

テスト駆動開発は、プログラミング中の不安をコントロールする手法だ。
-> 変数のタイポなどを教えてくれる

numericクラスmodulo
self を other で割った余り r を返します。

アルゴリズムの置き換え を適用します。 メソッドチェーンと述語メソッド を使って Ruby らしい書き方にリファクタリングしてみました。

述語メソッドとはメソッド名の末尾に「？」をつけたメソッドのこ

存在しないファイルをバイナリで
クラウドだとパスが作れない。
ストリームで流し込めて入る流すものなの？
テキストバイナリtxtファイル
アジュールファンクション
データを収集スラック
すごく長く
テキストファイルを作ったときの文字コード設定がutf-8じゃないとダメなんですが

メソッドのインライン化
byebug

<=> = 宇宙船演算子
true = 1
false = -1
equal = 0
比べられない = nil

変数のインライン化

ミュータブルな変数はバグを作り込む原因
一次変数を使わないようにする

マジックナンバーを置き換えよう
シンボル定数によるマジックナンバーの置き換え
MAX_NUMBER = 100
